# protobuf-gradle-plugin-623

Gradle test project for https://github.com/google/protobuf-gradle-plugin/issues/623

The project was created with `gradle init`. After that, the protobuf plugin was added.
