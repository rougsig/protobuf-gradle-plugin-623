/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package protobuf.gradle.plugin.app;

import protobuf.gradle.plugin.list.LinkedList;

import static protobuf.gradle.plugin.utilities.StringUtils.join;
import static protobuf.gradle.plugin.utilities.StringUtils.split;
import static protobuf.gradle.plugin.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
